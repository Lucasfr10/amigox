<div class="row show_group col-md-12">
  <a href="<%= "/events/" + event.id.to_s %>">
    <label class="col-md-12">
      <h5>
        <%= event.name %> <span class="badge"><%= event.users.count %></span>
      </h5>
    </label>
  </a>
</div>
